apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: prometheus-operator-kubelet-sync
  description: |
    Enable this role when you use --kubelet-service
rules:
- apiGroups:
  - ""
  resources:
  - nodes
  verbs:
  - list
  - watch
