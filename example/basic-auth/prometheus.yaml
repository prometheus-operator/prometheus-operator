---
apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prometheus
  labels:
    prometheus: basic-auth
spec:
  replicas: 1
  web:
    basicAuthUsers:
    - username:
        name: basic-auth
        key: username
      password:
        name: basic-auth
        key: password

