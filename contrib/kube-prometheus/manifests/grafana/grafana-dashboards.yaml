apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
data:
  dashboards.yaml: |+
    - name: 'default'
      org_id: 1
      folder: ''
      type: file
      options:
        folder: /grafana-dashboard-definitions/0
